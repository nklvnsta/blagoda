{
  "scripts": {
    "start": "cd frontend && npm install && npm run dev -- --host 0.0.0.0 --port 3000",
    "dev": "cd frontend && npm install && npm run dev -- --host 0.0.0.0 --port 3000"
  },
  "dependencies": {},
  "devDependencies": {}
}